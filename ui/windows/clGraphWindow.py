from ui.windows.graphWindow import GraphWindow


class CLGraphWindow(GraphWindow):
    window_title = "Control Line Graphs"
    graph_title = "BIAS current (CL current)"
    y_label = "BIAS Current, mkA"
    x_label = "CL Current, mA"
